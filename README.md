# Ants AI Challenge 2011 (Restoration Project) 🐜

Восстановление легендарного соревнования Google AI Challenge 2011 "Ants". 
Проект направлен на создание локальной среды для разработки, тестирования и проведения турниров между ботами.

## 🕹 Суть игры
Вы управляете колонией муравьев. Ваша цель — собирать еду, размножаться и уничтожать холмы противников. 
Игра проходит на прямоугольной карте (часто в форме тора) с туманом войны.

## 🐳 Docker Support
Проект полностью контейнеризирован. В образе предустановлены Python, Java и C++.

### Сборка
```bash
docker-compose build
```

### Запуск тестового матча
```bash
docker-compose run --rm engine
```

### Запуск своего бота
Если ваш бот на Java:
```bash
docker-compose run --rm engine bash -c "javac bots/my_bot/*.java && python3 engine/playgame.py --map_file maps/maze/maze_p02_01.map 'java -cp bots/my_bot MyBot' 'python3 engine/dist/sample_bots/python/RandomBot.py'"
```

---

## 🛠 Технологии
- **Engine**: Python (оригинальный движок соревнования).
- **Protocol**: Текстовый поток через stdin/stdout.
- **Support**: Multi-language (Java, Python, C++, C#, etc.).

## 🚀 Как начать (План)
1. **Infrastructure**: Настройка движка и инструментов визуализации.
2. **Restoration**: Интеграция старых ботов из архивов.
3. **Tournament**: Проведение локальных матчей.

## 📜 Протокол (Кратко)
Каждый ход сервер шлет боту состояние:
- `f <row> <col>` - еда в клетке.
- `a <row> <col> <owner>` - муравей игрока.
- `h <row> <col> <owner>` - муравьиная горка (hill).
- `w <row> <col>` - вода (препятствие).
- `go` - конец хода, ждем команд от бота.

Бот отвечает командами:
- `o <row> <col> <direction>` (N, S, E, W).
- `go` - конец списка команд.
